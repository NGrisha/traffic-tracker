services:
  traffic-tracker:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: traffic-tracker
    environment:
      YOLO_CONF: "0.35"
    volumes:
      - ./data:/app/data
      - ./models:/app/models

    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]


